services:
  api:
    build:
      context: .
      target: builder
      dockerfile: ./API/Dockerfile
    container_name: api-fastapi
    environment:
      PORT: 8000
    ports:
      - '8000:8000'
    restart: "no"

